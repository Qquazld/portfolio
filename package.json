{
  "name": "yann-portfolio",
  "version": "1.0.0",
  "scripts": {
    "dev": "live-server --open=index.html --port=3000",
    "build": "npx tailwindcss -i ./src/css/input.css -o ./dist/output.css --minify",
    "watch": "npx tailwindcss -i ./src/css/input.css -o ./dist/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.17"
  }
}
